// 1 - Pide al usuario dos números.Muestra el resultado de la resta de ambos, en pantalla y / o en consola

let x = parseInt(prompt("Ingrese un número"));
let y = parseInt(prompt("Ingrese un número"));

let resta = x - y; 10

alert(resta);